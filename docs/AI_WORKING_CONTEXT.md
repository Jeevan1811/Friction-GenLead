# Friction GenLead AI Working Context

Last updated: 2026-09-24 (Asia/Singapore)

## Repository and release state

- Repository: `C:\Users\Asus\OneDrive - wsm llc\Documents\Friction GenLead`
- Working branch: `codex/genlead-shipfix`
- Application commits: `627b2c5` (redirect ordering and streamed chat fallback), `484f700` (pin connections to validated DNS addresses)
- Branch base: `19580033d23e817357a3719dc3f11a7e30fd2fcb` (local main, including the committed postcode centroid helper)
- Production before this fix: `72754d9` on `31.97.70.50`, PM2 app user `frictiongenlead`
- Production after this fix: `484f700` on `main`; research API restarted, health returned 200, unauthenticated data access returned 401, and a real crawler GET to `https://example.com/` returned 200.
- The original `main` worktree has unrelated uncommitted migration and UI changes. They were not copied into this branch or changed.
- Deployment status: deployed and verified. A documentation-only follow-up may advance the repository HEAD without changing app code.

## Verified findings and changes in progress

- Verified: crawler previously allowed HTTPX to follow redirects before checking redirect destinations, and its separate HTTPX DNS lookup left a rebinding window.
- Fixed in `627b2c5` and `484f700`: redirects are followed manually; every destination is validated before its request, and the validated IPs are pinned at the TCP connection layer. Redirects are limited to five hops; HTTPX environment proxies are disabled for crawler requests.
- Verified: an LLM error during streamed chat returned an empty SSE body and omitted `[DONE]`.
- Fixed in `627b2c5`: bounded provider output is buffered so a midstream provider failure can return the built-in guide answer and `[DONE]` without exposing a partial answer.
- Live Google Sheets QA rows created during the earlier audit were removed and their absence verified: `comp-test`, `comp-test-2`, `loc-test`, and the rejection entry for `comp-test-2`.
- Test-driven evidence: redirect ordering and streamed chat failure tests failed before their fixes; the pinned-network-backend test failed before the DNS pinning implementation. All pass after implementation.
- Backend verification: `python -m pytest -q` completed with 70 passed before deployment. Production has `httpx 0.28.1` and `httpcore 1.0.9`, within the declared dependency bounds.

## Limits and next owner

- Real mailbox delivery and OTP completion still require a human with access to the recipient inbox. A prior production reset request returned 200; this confirms the app's mail send call succeeded, not that a person read the message.
- Independent Claude security review was blocked because the organization disabled Claude Code subscription access; no permission bypass was attempted.
- Next owner: Jeevan / a mailbox user to confirm receipt and completion of an OTP. Codex can handle follow-up fixes if the human inbox test finds an issue.

## 2026-09-25 — source workbook archive conversion and ship-readiness QA

- Truth labels: **VERIFIED** for source-cell preservation in native Google Sheets archives and existing canonical counts; **NOT READY TO SHIP** for the full MSV prospecting workflow; **UNKNOWN** for MSV's direct Drive access (no sharing changes were made).
- Reviewed all 12 pages of `C:\Users\Asus\Downloads\Precision_Prospecting_Workflow.pdf`. It calls for the cleaned Google Sheet to remain the app's live record and the original workbook to remain historical/untouched; it also specifies real postcode-to-company research, multi-source verification, human approval, contact-role discovery, avoiding repeat searches, and call follow-up/notes.
- The actual project data files and audited snapshot are byte-identical. Master workbook SHA-256: `331366136E82F293CEB55616494BBB3E09B90C8B815A4AF3B2CA8D052DB4D98C`; Targets workbook SHA-256: `A3373AE8F4040EFC924779ED2BEA075EFFD721081698555AD82BCC1ED73569DA`.
- Imported both originals as native historical Google Sheets, leaving local Excel files untouched: Master Customers (`https://docs.google.com/spreadsheets/d/1OHOTEgtEgXfOt5oLwEW5HxNW38QbBYNss8mNSwyLMh8/edit`) and Customer Targets (`https://docs.google.com/spreadsheets/d/1ksZc88skidpQuyB7NirjS3Ub_3HT8RWNSpNRFRcIwfs/edit`). All seven tabs were checked by bounded ranges: 30,490 nonblank cells match in value and source position, with zero differences. Native tables `QLDContactsTable` A1:I3360 and `NewOceaniaContactsTable` A1:K90, plus Target workbook filter/sort metadata, were preserved. Targets/Sheet1 has 3,418 used-range rows but only 134 populated rows; the import has 1,000 grid rows, so only the blank trailing extent was not expanded.
- Renamed the existing live spreadsheet to `Friction GenLead - MSV Live QLD Data` (`https://docs.google.com/spreadsheets/d/1LWY2AlvBGv9QRAb6gkFivw6rKVIg5Z0DsFLnF9glIkY/edit`) and added `Source Archive Manifest` with per-sheet counts, source hashes, parity results, and archive links. Existing app and staging data tabs were not rewritten.
- Canonical counts remain 4,199 companies, 6,331 locations, and 1,707 contacts; prior full-row/ID digest checks against the migration projection passed. Archives retain source values absent from normalized app tables: 347 landlines without contact names, 470 `Verification / Source` cells, 311 raw financial suffix values in `Sheet3`, and the postcode-only/no-company row at Targets/Master row 3569. These are preserved in the archive, not yet searchable app fields.
- Product gaps verified in release worktree `codex/genlead-shipfix` at `3e951b256ee29ba786907f7f03c263537f70a829`: postcode research still returns mock companies (`services/research/app/services/abr.py`); UI warns results are samples; no real contact finder is connected. Company `notes` display, but there is no call activity/follow-up date/reminder workflow. Search jobs/history are in memory, not durable across restart. The full MSV workflow is therefore not ready to ship as real lead generation.
- No app code, `.env`, VPS service, or Drive permissions were changed; no new application deploy was performed. Original `main` remains at `19580033d23e817357a3719dc3f11a7e30fd2fcb`, four commits behind origin with prior unrelated dirty changes intact.
- Next owner: Jeevan to choose/authorize real company/contact sources and credentials, decide the call-follow-up interaction, and confirm whether the archives should be directly shared with MSV. Codex can implement and test the app work in the release worktree before a separately verified deployment.

## 2026-09-25 — legacy workbook reconciliation

- Truth labels: **VERIFIED** for current live/staging values versus the saved migration projection; **SOURCE-FIDELITY GAP** for raw workbook fields not represented in those tabs.
- Read-only scope: the two source workbooks and migration artifacts under `C:\Users\Asus\OneDrive - wsm llc\Documents\Friction GenLead\scripts\migrate-legacy\data`; current Google Sheet tabs `Companies`, `Locations`, `Contacts`, and their three `_STAGING_*` counterparts. Original `main` worktree remained untouched.
- Re-parsed the current workbooks with `migrate.py`. Entity counts and row accounting match `migration_dry_run.json` after excluding generated `last_modified` timestamps: 4,199 companies, 6,331 locations, 1,707 contacts; 10,156 logical source records.
- Read all six current tabs and compared full row-value SHA-256 digests against the expected staged and live projections. All ID-set digests, counts, and row-value digests matched; no live data was written.
- Row accounting: 13,513 used-range row positions; 6 headers; 13,507 data-row positions; 3,791 blank/spacer rows; 9,715 primary company records plus 441 additional side-by-side company records. One nonblank source row with no company name is skipped at `Customers - Targets - QLD.xlsx`, `Master`, row 3569.
- Confirmed raw-source omissions: 347 business landlines on rows with no contact name are dropped by `assemble`; 470 populated `Verification / Source` values are collected into dry-run-only `verification_notes` but not written by `write_staging_to_sheets.py`; 311 `Sheet3` financial suffix values are stripped while only a warning flag survives. These fields do not appear in live or staging tabs.
- `raw_postcode`, `professional_url_raw`, provenance, and quality flags are present in staging but excluded from live canonical schemas. Thus live tabs match the migration projection, but the import is not lossless against every source cell.
- No workbook, application code, or Google Sheet was changed. Before repairing these gaps, owner direction is needed on whether raw values should remain in a dedicated audit/source tab or be added to the live GenLead schema/UI.

## 2026-09-25 — workflow implementation and release boundary

- Truth labels: **IMPLEMENTED IN ISOLATED WORKTREE** for interactive dashboard guidance, persistent activity/follow-up workflows, and durable search-run summaries; **VERIFIED** for automated tests/build/security audit; **NOT READY TO SHIP AS REAL PROSPECTING** because real company/contact providers and working OTP delivery are still missing.
- Worktree: `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`; branch `codex/genlead-shipfix`; base/HEAD remains `3e951b256ee29ba786907f7f03c263537f70a829`. Changes are uncommitted and isolated. Original OneDrive `main` checkout and its dirty work were not touched.
- Implemented: a route-aware 9-step dashboard tour that can be restarted from Settings; Google-Sheets-backed activity records and scheduled follow-ups with complete/reopen behavior; search-run summary persistence and interrupted-run recovery; explicit honest empty/source states. Company activity notes survive a failed write, and the API reports saved only after a live Sheets write succeeds.
- Safety correction: disconnected/mock ABR discovery, contact research, scoring, and verification now fail closed instead of presenting sample records as real. Human approval is labeled as user evidence, not as externally verified business data. New-company research submission responds unavailable until a real source is connected.
- Added `Activities` and `SearchRuns` tab definitions/adapters. These are code-only in this worktree; production Sheets have not been changed and will require the API's tab-creation path when a future approved deployment starts.
- Verification recorded: Python backend suite **81 passed**; web production build and TypeScript typecheck succeeded; `npm audit` reported **0 vulnerabilities**; `git diff --check` had no whitespace errors (only Windows line-ending warnings). Browser automation was not completed because the local web-server process launch was blocked; do not claim live UI click-through for this change.
- No `.env`, credential export, VPS service, live application Sheet, or external account was read/changed in this pass. No commit, push, or deployment was performed.
- Blocking release gaps: (1) no real ABR/company discovery or contact-finder provider, so prospecting is intentionally disabled; (2) production SMTP previously returned `535`, no reusable working sender was established, and no password reset or credential was changed; (3) no real inbox OTP setup/reset pass; (4) source-only workbook values remain recoverable in archives but not queryable in canonical app fields (347 landlines, 470 source/verification cells, 311 raw suffix values, plus one postcode-only row).
- Next owner: Jeevan to authorize/connect real company and contact data providers and provide a working transactional-email route without exposing credentials in chat. After those exist, Codex can run real provider/API/inbox QA, deploy only with current explicit authorization, verify the newly created Sheet tabs and PM2 health, and re-check source-field parity.

## 2026-09-25 — source-data upgrade and live-sheet sync (supersedes earlier sheet-gap status)

- Truth labels: **LIVE SHEET IMPORT VERIFIED**; **APP DEPLOYED AND SMOKE-TESTED**; **PROSPECTING/OTP PROVIDER GAPS REMAIN SEPARATE**.
- Scope: implement the planned Sheet upgrade so every source row/value is retained and searchable in GenLead, and dashboard pages refresh from the live Google Sheet. The two original Excel workbooks and their historical archive tabs remain untouched.
- Live spreadsheet: `Friction GenLead - MSV Live QLD Data` (`1LWY2AlvBGv9QRAb6gkFivw6rKVIg5Z0DsFLnF9glIkY`). Append-only/idempotent migration wrote and read-back verified 10,157 `SourceRecords` rows (10,156 linked, one unmatched). Existing canonical rows/values/order passed pre/post checks; only source-data columns were appended. The original archives and staging tabs were not overwritten.
- Source fidelity: all original nonblank values, including landlines without named contacts, verification/source values, financial suffixes, raw postcodes/URLs, provenance, quality flags, and the postcode-only unmatched row, are available in the raw source-record archive and applicable canonical search fields. The re-run is idempotent: 10,157 already imported, zero pending, zero conflicts.
- App changes: commit `ca5a6fbb52f985378882d24aa38ac09ce3f56bda`, merged as `72c7093851123ac263ecc7a1490c9c1aed0436e0` via [PR #1](https://github.com/Jeevan1811/Friction-GenLead/pull/1). Header-name-based Sheets reads, append-only schema extension, non-destructive partial cell writes, protected paginated source-record search, sync count, source-data UI, and visible-page/focus refresh (30-second interval) are included, along with Sheet-backed search/follow-up/activity features, Settings-startable dashboard guide, and fail-closed sample research paths.
- API verification against live Sheets adapter: 10,157 source rows returned; unmatched row visible; searches find source-only values; sync status exposes the source-row count. Backend suite: 84 passed. Next.js production build/typecheck passed, including `/source-data`, `/follow-ups`, and `/settings` routes.
- VPS deployment verified: `31.97.70.50`, `/opt/frictiongenlead/app`, `main` at `72c7093`; only `frictiongenlead-web` and `frictiongenlead-api` were restarted. Both PM2 processes are online; API `/internal/health` returned 200; public `/login` returned 200; `/source-data` redirects unauthenticated users to login; protected source-record and sync-status endpoints return 401 without a session. Existing untracked production files (`ecosystem.config.js`, virtualenv and SMTP backup) were preserved.
- Production build passed on the VPS. `npm ci` emitted a non-blocking engine warning because Cesium packages request Node >=22 while this host uses Node 20. The Next production build completed successfully. No authenticated production session was available for a post-deploy row-level browser check; the same live Sheet adapter and protected endpoints were verified before deployment.
- This Sheet-sync release does not create a real third-party prospect/contact data provider and does not fix the previously observed SMTP `535`; no SMTP credentials were changed. These are separate from preserving, searching and refreshing MSV's existing real workbook data. Do not describe the complete external research/email workflow as ready until separately integrated and tested.
- Next owner: Jeevan to arrange a working transactional-email sender and approve a real prospect/contact research provider if he wants new outbound research. Then complete the real inbox OTP flow and an authenticated dashboard/source-data QA pass. Those are distinct from the now-deployed preservation and live-Sheet refresh work.

## 2026-09-25 — SMTP access-log diagnosis and prospect-search clarification

- Truth: **SMTP AUTH FAILURE CONFIRMED IN HOSTINGER LOGS**; **NO CREDENTIAL OR VPS CONFIG CHANGED**; **NEW-COMPANY SEARCH IS NOT AVAILABLE IN PRODUCTION CODE**.
- With Jeevan signed into hPanel, inspected the `friction.com.my` mailbox and filtered Email Logs to SMTP + Failed. Recent attempts from the GenLead VPS (`31.97.70.50`) for `management@friction.com.my` are labeled `Incorrect password` (latest visible: 2026-09-23 00:55:11). Older VPS attempts show the same. Mailbox status is Active; mailbox settings show Disable mailbox, Suspend access, Suspend receiving, and Suspend sending all off. This rules out the suggested hPanel suspend toggle.
- Safe runtime inspection performed earlier in this task: Next's actual dotenv parser preserves the quoted/escaped SMTP password literal; no secret value was printed. Runtime host/port/from align with Hostinger's published config (`smtp.hostinger.com`, 465, From matches authenticated user). Evidence points to the credential currently supplied by the app being rejected, not dotenv truncation or a disabled-mailbox setting. Hostinger app-password generation and secret replacement were not performed.
- Product clarification: imported Excel/Google Sheet records remain a distinct, real, searchable dataset; they do not supply additional businesses beyond the workbook. `services/research/app/routers/operations.py` hard-returns 503 for new postcode research, `/internal/discover` also returns 503, and `services/research/app/services/abr.py` has no live ABR implementation. The core external new-company discovery function is therefore not working; this should not have been characterized as full prospecting readiness.
- A real ABR integration is a possible free company-registry layer, but requires the owner to accept ABR's web-services agreement and obtain its GUID. ABR supplies registry names/ABN/status/state/postcode, not contact details or industry; it cannot alone fulfill MSV's contact-finding workflow. No provider registration or paid-service commitment was made.
- Fixed an additional latent Jev pipeline bug in this isolated worktree: contact-research and evaluation methods had been nested under `_safe_int` and were unavailable on `Jev`. Added a regression assertion; full research-service suite now passes **85 tests**. This code fix is uncommitted and not deployed, and does not enable discovery without a real provider.
- No inbox content was opened. No password/app password was created, no `.env` file edited, no SMTP retry triggered, and no VPS service restarted. Current repo: `codex/genlead-shipfix` at `ad372e734167b4b06b639fe1aba492c942239d43`; only prior untracked `.playwright-cli/` retained.
- Next owner/action: Jeevan must create and privately install a separate revocable Hostinger app password (do not send it in chat) or select another authorized transactional sender; then Codex can inspect sanitized logs and verify OTP delivery. For prospecting, Jeevan must approve/register an appropriate data source. Codex should implement and test the live ABR company-candidate path after an ABR GUID is available; a separate approved source is still required for contact discovery/enrichment.

## 2026-09-25 — prospecting workflow source audit and false-success fix

- Truth labels: **SOURCE AUDIT VERIFIED**; **86 BACKEND TESTS PASS**; **LIVE PROSPECTING NOT AVAILABLE**; **NOT DEPLOYED**.
- Re-inspected all 12 visual pages of `C:\Users\Asus\Downloads\Precision_Prospecting_Workflow.pdf`. The blueprint calls for postcode-based real QLD company discovery; cross-source ABR/public-company/company-site/public-web/location checks; decision-maker research from company/team pages, public web, and publicly discoverable professional profiles (without mass LinkedIn scraping); repeat-search suppression; human review; and Google Sheet updates. It describes source categories, but no actual provider endpoint, API credentials, or search service.
- History explains the UI change: the original `b3852f0` implementation returned hard-coded/mock companies and contacts. Commit `ca5a6fb` intentionally replaced those demo responses with 503 guards to stop fictitious prospects being presented as real. The guards are still present in `operations.py` and `discovery.py`; `search/page.tsx` also hardcodes `researchSourceReady = false`.
- Real infrastructure exists but is narrower than the product flow: `WebsiteCrawler.fetch()` performs a real single-URL HTTP request with DNS/IP validation, pinned connections, redirect validation, rate/page/size limits. `crawl_site()` still fetches only the home page; `extract_contacts()` extracts `mailto:` addresses only and does not discover names, roles, phones, team pages, or public profiles. `ABRAdapter.is_configured` returns false and all ABR lookup/search methods fail closed. Jev's discover stage calls only this ABR stub; its verify stage is ABN-only; its contact stage gets a website URL only from discovery. The public web-search/location-evidence sources in the blueprint are not integrated.
- Persistence is partly implemented: existing workbook/source data and user-entered Sheets workflows are live; research history stores summary counts only. New research candidates/results are not written to canonical Sheets by the research pipeline, and repeat-search suppression against existing/approved/rejected records is not connected.
- `python -m pytest -q` in `services/research`: **85 passed before the fix**. `test_workflow_safety.py` explicitly asserts the research/discovery/contact endpoints return 503, and that the ABR postcode adapter raises `not implemented`; green tests therefore prove safe refusal, not live prospecting readiness.
- Fixed a proven latent false-success bug in `_run_pipeline`: a failed discovery step previously allowed the empty job to continue and end as `completed`. The pipeline now marks the job `failed`, records a summary error, and skips downstream steps when discovery does not complete. Added a regression test; full backend suite now **86 passed**. The existing uncommitted Jev method-indentation repair is retained and separately tested.
- No `.env`, provider credential, live data, Google Sheet, VPS service, or deployment was touched. Current repo is `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; source/docs/test changes remain local and uncommitted; pre-existing `.playwright-cli/` remains untouched.
- Next: implement against an owner-approved live postcode/web-search source and ABR access (ABR web services require the registered GUID/acceptance); complete compliant multi-page company-site and contact extraction, source evidence, cross-checking/deduplication, candidate persistence and human-review flow; only then enable the UI and run provider-backed QA. Do not call new-prospect search ready or deploy while these are absent.

## 2026-09-25 — public-scraping workflow implementation (latest status)

- Truth labels: **PUBLIC ABR SCRAPER LIVE-READ VERIFIED**; **WEBSITE CRAWLER LIVE-READ VERIFIED**; **107 BACKEND TESTS PASS**; **FRONTEND PRODUCTION BUILD PASS**; **NOT DEPLOYED**; **NOT FULLY AUTOMATED/SHIP-READY**.
- Implemented in this isolated worktree: public ABR postcode/name scraping without the ABR GUID, checksum validation, cancellation/status handling, duplicate suppression against live canonical/rejected Sheet rows, candidate/location/source-provenance persistence, durable search-result rehydration, honest unverified industry/site states, a dashboard search-results view, and a Company-drawer action to crawl a company site for explicitly published people/channels. Existing website fields are not silently replaced; a new URL is robots-checked, SSRF-checked by the crawler, persisted as operator-supplied/unverified, and only then crawled. Contact writes report whether every generated row was synced. A failed live Companies/Rejected tab read now halts discovery before it can write undeduplicated candidates.
- Read-only source checks: one ABR query for QLD postcode `4000` + Mining returned **87 active name matches** across its bounded name terms. This is not a complete sector list: ABR name matching is not industry classification and provides no website/contact people. A read-only crawl of AE Group's public site fetched four allowed same-origin pages; it extracted zero explicit name+role people and 12 generic public contact channels. No contact values were copied into this note.
- Automated general search did not become a runtime source: DuckDuckGo's ordinary browser page presented a human CAPTCHA challenge in headless automation; no challenge was solved or bypassed. Its `/html` and `/lite` paths are disallowed in robots.txt. The shared public Overpass instance's published usage guidance says commercial use should use self-hosted or paid servers, so it was not wired into this commercial prospecting app. Sources checked: https://duckduckgo.com/robots.txt and https://wiki.openstreetmap.org/wiki/Overpass_API.
- The working flow is real but bounded: discover ABR name candidates; for an operator-supplied/saved official website, crawl robots-allowed same-origin pages and persist source-backed, unverified contacts to Sheets. It does **not** automatically discover a candidate's website, guarantee sector fit, cover every company, scrape mass social profiles, or produce a complete verified contact list. The browser action accepts an operator-supplied URL because ABR does not return one.
- Verification: `python -m pytest -q` from `services/research` — **107 passed**. `npm run build --workspace=apps/web` — passed, including `/companies` and `/search`. `git diff --check` — passed (Windows line-ending notices only). Production browser click-through remains unavailable because the live OTP SMTP path has a known `535`; no local authenticated browser flow was completed.
- No `.env`, credential, live Sheet row, VPS process, production app, or user data was changed. No deployment/commit/push occurred. Worktree `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, base HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; all task changes remain local/uncommitted. Existing `.playwright-cli/` was preserved.
- Next: choose a permitted, production-suitable web-search runtime (or authorize operating a self-hosted/paid source) to auto-resolve official company sites from ABR candidates; then test against live Sheet writes and signed-in dashboard. Separately repair OTP sender credentials and complete the real inbox flow. Until automated company-site discovery exists, describe the site crawl as operator-assisted, not end-to-end automated prospect research.

## 2026-09-25 trusted-browser auth update

- Current repo: `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`. HEAD is an ancestor of `origin/main` (`db25261`); there are two remote-main commits after this branch. Existing scraper/auth/worklog edits are uncommitted. Preserve `.playwright-cli/` and all other pre-existing edits.
- Implemented, source-only: production allowlist remains configuration-driven with no signup path; trusted-device file-backed enrollment after email OTP; other browsers are denied before OTP mail/reset when possible; sessions require matching signed JWT, secure HttpOnly browser cookie, email and current store; logout preserves trust; rolling 365-day session refreshes on app load, visibility return and every 12 hours. `AUTH_ALLOWED_EMAILS` must be changed in production to exactly `Mathysuthen@yahoo.com.au` at deployment, touching only that variable. First successful OTP after deployment permanently claims that browser until manual recovery.
- Shared store defaults to `apps/web/data/trusted-device.json` in Next and FastAPI, mode 0600 and gitignored. Custom `AUTH_TRUSTED_DEVICE_STORE_PATH` must be identical in both processes; both must run as `frictiongenlead`. If the browser cookie is lost, an administrator must verify MSV and remove the exact trust record; MSV then completes OTP on the intended browser. This binds a browser-cookie installation, not hardware attestation.
- Migration consequence: old session JWTs without device claims are rejected; one OTP re-login is required. Do not deploy/allow first login from Jeevan's current-profile browser unless the owner explicitly chooses it. Await which browser MSV will use first.
- Verification: `python -m pytest -q` in `services/research`: **112 passed**; Node trusted-device tests: **4 passed**; Next production build passed; web typecheck passed sequentially after the build; `git diff --check` has no errors (line-ending warnings only). The browser QA tool exposed only an isolated `about:blank`, not the authenticated in-app page. No live session, `.env`, cookie, production VPS, Sheet, SMTP setting or credential was touched.
- Claude's guarded read-only security review was attempted but unavailable because the organization disabled Claude subscription access. No independent review result is claimed.
- Release remains incomplete: this auth implementation is not deployed and no trusted browser enrolled; SMTP delivery was not re-tested; automatic public-web company-site discovery is still missing, although public ABR discovery and operator-assisted official-site crawling are implemented in uncommitted code. Confirm intended browser and current OTP status, bring branch safely up to current `origin/main`, run final QA, then deploy only GenLead with the single-email allowlist. MSV must authenticate on his intended device first.

## 2026-09-25 — current production QA: global search/map

- This entry supersedes older SMTP statements above: Jeevan reports that OTP codes are arriving for him. Treat the 2026-09-23 SMTP 535 as historical, not a current blocker. No OTP was requested or retried in this QA.
- Read-only in-app production check at `https://frictiongenlead.friction.com.my/search` shows the deployed UI is still Queensland/postcode-only and displays “New prospect searches are not connected yet” / “Research not connected.” The deployed `/locations` page still says “Operational sites across Queensland” and “Map view unavailable.” Browser console confirms the old Cesium chunk fails with `SyntaxError: Octal escape sequences are not allowed in template strings` followed by a `ChunkLoadError` for chunk 259.
- Local source changes implement free-text worldwide location search, bounded OpenStreetMap candidate discovery with industry-tag filtering and optional QLD ABR, provenance-aware candidate/contact persistence to Sheets, and a Leaflet world map. The dashboard notice now accurately calls source search best-effort and non-exhaustive. These changes are **LOCAL/UNCOMMITTED**, not in production.
- Final local verification: `python -m pytest -q` in `services/research`: **123 passed**; `npm run typecheck --workspace=apps/web`: **passed**; `npm run build --workspace=apps/web`: **passed**, including `/search` and `/locations`. Production search was not submitted; no Sheet rows were written.
- Source reliability is a release blocker: realistic bounded company/industry queries to both public Overpass endpoints timed out/returned HTTP 504 from the production VPS; only tiny diagnostic queries returned quickly. Mocked end-to-end tests do not establish live global discovery. OSM coverage is community-mapped and uneven; it is not a complete company/contact directory. See [Overpass API](https://wiki.openstreetmap.org/wiki/Overpass_API) and [OSM tile policy](https://operations.osmfoundation.org/policies/tiles/).
- No production deployment, `.env`/credential access, VPS restart, Google Sheet write, OTP send, commit, or push occurred. Branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; worktree contains prior user-owned uncommitted changes and `.playwright-cli/` remains untouched; `origin/main` is two commits ahead. Graphify graph was absent, so scoped repository search was used.
- Release status: **NOT READY TO SHIP**. Need a reliable permitted runtime source or approved self-hosted/paid Overpass before enabling prospect search in production, then run provider-backed search and Sheet-persistence QA. Global UI and map fixes are built/tested locally but undeployed. First trusted-device enrollment remains unperformed; do not deploy broader auth changes until MSV identifies and uses his intended browser.

## Current handoff — global prospect search release (2026-09-25)

- Scope: ship worldwide search/map and candidate-to-Sheets workflow; keep the requested single-trusted-device auth changes and session lifetime changes out of this release because the owner chose to defer device lock. User reports OTP currently arrives; no SMTP change or OTP send is part of this deployment.
- Code uses the latest monthly global Overture Maps Places release, bounded 5 km DuckDB/Parquet queries, curated industry categories, source/release/confidence/attribution/license provenance, and the existing geocoder + SSRF/robots-aware company-site crawler. Search deduplicates against canonical and rejected Sheet records before adding at most 30 candidates and their Locations/Contacts. Listed business phone/email are saved to new append-only Companies columns. The global Leaflet map replaces the broken Cesium bundle. Search, saved-company details, dashboard guide, and assistant descriptions were updated.
- Local validation: research backend **128 passed**; frontend typecheck **passed**; production build **passed**. Independent Antigravity/Gemini Flash-Lite static QA was run via the guarded router (report at `C:\Users\Asus\.gemini\antigravity-cli\brain\98ee2be3-5433-4fec-8425-3060b368a75d\overture_qa_review.md`, truth `SOURCE-ONLY`). Its stale ODbL fixture example was corrected to Meta/CDLA after checking current Overture sources. Official references: https://docs.overturemaps.org/guides/places/ ; https://docs.overturemaps.org/schema/reference/places/place/ ; https://docs.overturemaps.org/attribution/ .
- No real search submitted, no Google Sheet rows changed, no auth lock or session changes deployed, no `.env`/credential read or modified, and no OTP sent. The browser's only currently open tab is the MSV Google Sheet; there is no authenticated GenLead dashboard tab available to Codex at this moment.
- Repository: `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`; branch `codex/genlead-shipfix`; base HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; `origin/main` is `db25261` with the same tree. Existing `.playwright-cli/`, trusted-device files and unrelated user changes must remain untouched. Production VPS target is `/opt/frictiongenlead/app`, current commit `72c7093851123ac263ecc7a1490c9c1aed0436e0`, run as `frictiongenlead`; untracked `ecosystem.config.js`, Python venv and SMTP backup must be preserved; restart only `frictiongenlead-api` and `frictiongenlead-web`.
- Release status: **LOCAL VERIFIED; DEPLOYMENT PENDING**. Post-merge: install only declared web/Python dependencies, rebuild, restart the two named processes, check health and browser pages, and run the deployed adapter's live source query without Sheet writes. Authenticated candidate persistence in the production Sheet remains unverified until MSV signs in and authorizes a real location/industry search; do not add fake test data.

## Current release correction — 2026-09-25

- PR #3 (`4a20533`) is merged and its tree is on the VPS; npm/Python dependency setup and the Next build succeeded. Neither named PM2 process has been restarted yet.
- A pre-restart, read-only live Overture smoke query exposed unsupported DuckDB config key `enable_progress_bar`; DuckDB raised before returning results. No search candidates were produced and no Sheet operation occurred.
- Local fix removes that key; regression test added. Research backend suite: **129 passed**. This narrow fix still needs merge/deployment and a successful repeat of the live query before restarting the API/web processes.
- Keep auth/device-lock changes out of scope (owner explicitly deferred them). Do not inspect `.env`/credentials, send OTP, or claim authenticated candidate persistence verified. Preserve the three pre-existing untracked VPS items and restart only GenLead's API/web PM2 processes.
- After PR #4 was pulled, a repeat read-only query reached Overture release `2026-09-23.0` and found a second mismatch: Place schema v2 has top-level `basic_category`, not `taxonomy.basic_category`. This query returned no candidate rows; there was no Sheet operation or service restart. Local SQL now uses the documented top-level field and regression test asserts that contract; backend suite: **129 passed**. Merge this narrow correction and re-run the live query before restarting.
- After PR #5, live schema binding then exposed `addresses[1].country_code`, which is absent from the current Places Address struct. Local fix selects `addresses[1].country`, derives the ISO alpha-2 code, and falls back to the geocoded country name for display. Targeted tests plus all **129 research tests** pass; correction remains unmerged. The live query still returned no candidate rows, and no Sheet operation or service restart occurred.
