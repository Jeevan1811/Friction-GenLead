# Friction GenLead Worklog

## 2026-09-24 — SSRF redirect and streamed chat fixes (deployed)

- Truth labels: **Verified**, **DEPLOYED**.
- Repository: `C:\Users\Asus\OneDrive - wsm llc\Documents\Friction GenLead`.
- Branch: `codex/genlead-shipfix`, based on commit `19580033d23e817357a3719dc3f11a7e30fd2fcb`.
- Production baseline: commit `72754d9`; web and research API PM2 processes were online before this change.
- Release commits: `627b2c5` and `484f700`, pushed to `main` and pulled onto the VPS.
- Fixed crawler redirect ordering and pinned the validated DNS address at the TCP backend, closing both redirect-following and DNS-rebinding paths.
- Fixed streamed chat provider failures to return the built-in fallback and terminal SSE event without exposing partial output.
- Added regression tests. Confirmed RED before implementation and GREEN afterward.
- Verification: `python -m pytest -q` — **70 passed**; `git diff --check` — clean.
- Production runtime dependencies: HTTPX 0.28.1 and HTTPcore 1.0.9 satisfy the declared requirements.
- Production verification: `frictiongenlead-api` restarted and remained online; `/internal/health` returned 200; protected `/internal/data/companies` returned 401 without a session; a real crawler GET to `https://example.com/` returned 200 HTML.
- Removed and re-read to verify the four QA artifacts from the live Sheets tabs: two company rows (`comp-test`, `comp-test-2`), one location row (`loc-test`), and the matching rejection row.
- Existing uncommitted work in the user's original `main` worktree was left untouched and excluded from this branch.
- Unknown: inbox receipt and human completion of OTP remain unverified.
- Independent Claude security review was unavailable because organization policy disabled Claude Code subscription access; the configured router reported that boundary, and no bypass was attempted.
- Next owner: Jeevan / a mailbox user to confirm OTP receipt and complete a real setup/reset flow.

## 2026-09-25 — source archives synced; app readiness gaps confirmed

- User authorized preserving/syncing the Excel data. Per the supplied PDF, the cleaned Google Sheet remains the app's live record and the source workbook stays historical, so the raw workbooks were imported as clearly labeled native Google Sheets archives, separate from app-write tabs.
- The actual source workbooks under `C:\Users\Asus\OneDrive - wsm llc\Documents\Friction GenLead\scripts\migrate-legacy\data` match the audited snapshot byte-for-byte. SHA-256: master `331366136E82F293CEB55616494BBB3E09B90C8B815A4AF3B2CA8D052DB4D98C`; targets `A3373AE8F4040EFC924779ED2BEA075EFFD721081698555AD82BCC1ED73569DA`.
- Native archives: Master Customers `1OHOTEgtEgXfOt5oLwEW5HxNW38QbBYNss8mNSwyLMh8`; Customer Targets `1ksZc88skidpQuyB7NirjS3Ub_3HT8RWNSpNRFRcIwfs`. Compared every nonblank cell across all seven tabs, including source coordinates: 30,490 cells, zero mismatches. Confirmed tables `QLDContactsTable` A1:I3360 and `NewOceaniaContactsTable` A1:K90, plus Target workbook filter/sort metadata. Target/Sheet1 used-range extends to row 3,418 but populated values end at row 134; only the blank-only tail was not expanded.
- Renamed the live spreadsheet `Friction GenLead - MSV Live QLD Data` (ID `1LWY2AlvBGv9QRAb6gkFivw6rKVIg5Z0DsFLnF9glIkY`) and added `Source Archive Manifest` with links. Existing `Companies`, `Locations`, `Contacts`, `Rejected`, `SyncLog`, and staging rows were not edited. Canonical counts remain 4,199 / 6,331 / 1,707; prior full migration-projection checks still hold.
- Source-only values are now recoverable in archives but not searchable in canonical app tables: 347 landlines without a contact name, 470 verification/source cells, 311 raw financial suffix values, and one no-company postcode row (`Customers - Targets - QLD.xlsx`, `Master`, row 3569). Do not map these into user-owned notes without a field/meaning decision.
- Requirements QA against the 12-page PDF: Google Sheets persistence and manual approve/reject exist, but live postcode/company research does not—the ABR adapter and contact finder use mock/unconnected data. Generic company notes display, but call logging and follow-up scheduling are absent. Search jobs/history are in memory rather than Sheet-backed, so repeat-search suppression is not durable. **Not ship-ready as real lead generation** until real sources and durable follow-up/history behavior are implemented and verified.
- No app source, `.env`, VPS, or Drive permissions changed; no new application deployment was performed. Original `main` remains at `19580033d23e817357a3719dc3f11a7e30fd2fcb`, four commits behind origin with unrelated dirty changes intact.
- Next owner: Jeevan to select/authorize real ABR/contact sources and credentials, choose call-note/follow-up semantics, and confirm any direct Drive sharing with MSV. Codex to implement/test in `codex/genlead-shipfix`, then run release QA and deploy only approved app changes.

## 2026-09-25 — legacy workbook reconciliation (read-only)

- Truth labels: **VERIFIED** for mapped values in current live/staging tabs; **SOURCE-FIDELITY GAP** for identified raw workbook omissions.
- Re-parsed both legacy workbooks in the original `main` checkout and compared to the saved migration projection. Counts and row-accounting match after excluding generated `last_modified` timestamps: 4,199 companies, 6,331 locations, 1,707 contacts, and 10,156 logical source records.
- Read current `Companies`, `Locations`, `Contacts` and `_STAGING_*` tabs through Google Drive. Full row-value SHA-256 digests and ID-set digests matched the expected projection on all six tabs. No Google Sheet writes were made.
- Source rows: 13,507 data-row positions, including 3,791 blank/spacer rows; one nonblank no-company row is skipped (`Customers - Targets - QLD.xlsx`, `Master`, row 3569). The 441 second-list entries in `Master` are accounted for as additional logical records.
- Gaps proven in source/parser/writer: 347 landlines on company rows lacking a contact name are dropped; 470 nonempty `Verification / Source` cells are only in dry-run `verification_notes` and are not written to staging/live; 311 financial values appended to `Sheet3` names are stripped and not retained as values. Live schema also omits raw postcode, raw professional URL, migration provenance, and quality flags that remain in staging.
- Impact: current canonical rows exactly match the transformed projection, but this does **not** establish zero-loss source fidelity. Recovery requires the source workbooks and a clear destination for omitted fields.
- Prevention/closure: before any live data/schema migration, get owner direction on an audit/source tab versus adding fields to the live app schema, implement lossless mapping and a no-drop row invariant, then re-read and digest-verify every affected live/staging row. Do not modify live data until that decision is approved.
- Project code, source workbook files, and Google Sheets were not modified in this audit. Original `main` dirty changes remain untouched. Current release worktree: `codex/genlead-shipfix` at `3e951b256ee29ba786907f7f03c263537f70a829`; documentation changes are in that worktree only.

## 2026-09-25 — workflow implementation and final readiness status

- Truth: **IMPLEMENTED IN ISOLATED WORKTREE**; **NOT DEPLOYED**; **NOT READY** for real prospecting.
- Repo: `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, HEAD `3e951b256ee29ba786907f7f03c263537f70a829`; all current implementation changes remain uncommitted. The original shared `main` checkout and unrelated dirty files were left intact.
- Shipped in code (not production): Settings-accessible 9-step route-aware dashboard guide; activity/call/email/meeting/note logging; optional scheduled follow-ups with complete/reopen; follow-up list; Google Sheets adapters for `Activities` and `SearchRuns`; durable search summaries and interrupted-run recovery; honest unavailable/empty states.
- Made fake/disconnected research paths fail closed. No generated/sample businesses or contacts are represented as discovered/verified; research submit, contact discovery, scoring, and external verification remain unavailable until real providers exist. Human review decisions no longer imply external verification.
- Verification: `python -m pytest -q` — **81 passed**; Next production build — **passed**; web TypeScript check — **passed**; `npm audit` — **0 vulnerabilities**; `git diff --check` — no whitespace errors (line-ending warnings only). Browser click-through did not complete because the local web process could not be launched under the execution environment; UI behavior is build/test verified, not browser verified.
- No `.env` or secrets were inspected or edited, no SMTP/Hostinger password was changed, no production service or live Google Sheet was modified, and there was no push/deploy. The new Sheets tabs are not yet created in production.
- Remaining ship blockers: real ABR/company-source and contact-finder integrations; resolved production OTP mail delivery (earlier SMTP `535`) and a human test of code receipt/setup/login/reset; app-level access/mapping for the source-only fields identified in the workbook audit. Exact source values remain preserved in the two archive Sheets, with 30,490 nonblank source cells matched, but preservation in archives alone does not make all those fields available in GenLead.
- Next owner: Jeevan to supply/authorize a real prospecting provider and an approved working transactional-email sender. Then run real provider and inbox E2E QA; only after that should the isolated changes be promoted and deployed under the existing production authorization.

## 2026-09-25 — lossless source-data import and dashboard sync (supersedes earlier source-field gap)

- Truth: **LIVE SHEET DATA VERIFIED**, **APP DEPLOYED AND SMOKE-TESTED**, **NEW RESEARCH/OTP PROVIDERS STILL SEPARATE**.
- Applied `scripts/migrate-legacy/sync_source_records_to_live.py --apply` to the live MSV spreadsheet. It appended source fields and 10,157 raw `SourceRecords` rows (10,156 linked plus one unmatched). Read-back hash/row verification passed. Re-running the migration found all 10,157 already present, zero new rows, and zero conflicts.
- Existing canonical values and header order passed pre/post preservation checks. The import did not rewrite original workbook archive tabs, staging tabs, existing user notes or canonical values. All nonblank source cells (30,490 across the source workbooks) were already parity-checked against the native archive copies.
- Wired raw-source pagination/search and sync count through the API; canonical tables expose source-only landlines, verification/source text, financial suffixes, postcode, raw URL, provenance and quality flags. Added the Source Data screen, visible 30-second Sheets polling plus focus refresh, and navigation/help/tour links. Partial row updates now write only selected fields, protecting formulas and user-added columns.
- API adapter live-data checks: 10,157 rows, one unmatched row, searches for preserved source values succeed, sync status reports source rows. Backend tests: **84 passed**. `npm run build --workspace=apps/web`: passed, with type validation and `/source-data`, `/follow-ups`, `/settings` routes.
- Deployment baseline inspected: VPS app commit `3e951b2`; GenLead web/API PM2 processes online, bound to their app-specific ports behind `frictiongenlead.friction.com.my`. Production checkout has user-owned untracked `ecosystem.config.js`, venv, and SMTP backup; preserve them. Other PM2 apps are out of scope.
- Published commit `ca5a6fbb52f985378882d24aa38ac09ce3f56bda` on `codex/genlead-shipfix`; merged PR #1 as `72c7093851123ac263ecc7a1490c9c1aed0436e0`.
- Deployed only `/opt/frictiongenlead/app` and `frictiongenlead-web` / `frictiongenlead-api`; the VPS checkout is on `main` at `72c7093`. Existing untracked ecosystem config, virtualenv, SMTP backup, and all other PM2 apps were left untouched.
- `npm ci` completed with a non-blocking Cesium Node >=22 engine warning on the VPS's Node 20 runtime. The production Next build and type validation succeeded.
- Post-deploy smoke checks: both named PM2 processes online; API health `200`; public login `200`; protected `/source-data` redirects with `307`; unauthenticated `/internal/data/source-records` and `/internal/data/sync-status` return `401` as expected. No authenticated production browser session was available, so a live signed-in UI read after deployment remains unverified; adapter-backed reads against the actual Sheet passed before deploy.
- The deployed Sheet upgrade preserves/searches the existing Excel data and refreshes visible dashboard pages from Sheets every 30 seconds and on focus/return. It does not add an external real-company/contact provider or repair SMTP `535`; no SMTP settings/password were changed. These are separate from the source workbook data, which is now imported and accessible through the app's protected routes.
- Next: complete one authenticated `/source-data`/search/dashboard QA pass when a valid session is available; separately resolve SMTP delivery and decide whether to connect a paid/approved prospect research provider.

## 2026-09-25 — SMTP access logs and new-company search (diagnosis)

- Truth: **SMTP credential rejection verified in Hostinger logs**; **no credential, env, or VPS changes made**; **new external company search remains disabled**.
- In hPanel (read-only), SMTP + Failed access logs show repeated `Incorrect password` for the GenLead VPS source IP to `management@friction.com.my` (latest visible 2026-09-23 00:55:11). The mailbox is Active and all four mailbox disable/suspend switches are off. The existing safe runtime check shows Next dotenv parses the escaped literal as intended and host/port/from match Hostinger's official `smtp.hostinger.com:465` configuration. The current SMTP credential itself is rejected; no SMTP retry was sent.
- This explains why OTP email does not send; it is not a regression in the password feature. No app password was generated, main mailbox password was not reset, `.env.production` was not read back or edited in this step, and PM2 was not restarted. Next step requires Jeevan to create/install a separate revocable app password privately (not share it in chat), or choose an alternative sender; then repeat the OTP test.
- The dashboard can search the real imported source data but cannot discover companies outside it: `/internal/ops/research` and `/internal/discover` currently return 503; `ABRAdapter` is intentionally unimplemented. This is a real missing core feature, not merely an activation toggle. ABR's free web-service API requires owner acceptance of an agreement and a registered GUID; it returns registry facts, not business contact details or industry. No registration, provider account, or paid plan was created. To complete prospecting, implement ABR company candidates after the owner supplies a GUID and add a separate approved contact/enrichment source.
- Fixed a latent pipeline bug in this isolated worktree: `_step_research_contacts` and `_step_evaluate` had been accidentally nested inside `_safe_int`, so `Jev` had no such methods. Added a regression check; full research-service test suite is now **85 passed**. This fix is uncommitted/not deployed and does not activate the still-unconfigured research source.
- Repo evidence: branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; only pre-existing untracked `.playwright-cli/` left untouched. No production deployment or live-sheet write occurred during this diagnostic.

## 2026-09-25 — prospecting backend audit; failed-discovery status fix

- Truth labels: **SOURCE AUDIT VERIFIED**; **86 BACKEND TESTS PASS**; **LIVE PROSPECTING NOT AVAILABLE**; **NOT DEPLOYED**.
- Re-read all 12 pages of the visual prospecting workflow PDF. It requires real QLD postcode discovery, multi-source ABR/public-web/company-site/location verification, contact-role research, duplicate/repeat-search avoidance, human approval, and Sheet updates. It names source categories, not a specific live provider endpoint or credentials.
- Confirmed via Git history: initial `b3852f0` routes/data were fixture/mock-driven. `ca5a6fb` deliberately replaced those responses with fail-closed 503s. This was a guard against presenting fake prospects, not an outage or a hidden live integration.
- Confirmed exact state: `/internal/ops/research` and `/internal/discover` return 503; contact research and external verification/evaluation are similarly guarded; the UI has `researchSourceReady = false`. `ABRAdapter` has no network implementation and `is_configured` is always false. The crawler has a real, SSRF-hardened one-URL fetch, but site traversal is still one page and contact extraction is `mailto:` only. There is no web-search adapter, no team/profile/phone extraction, no pipeline write of discovered candidates to Sheets, and no existing-record/rejected-record dedupe in the research path. Sheets-backed historical run counts do not retain result details.
- An independent read-only Gemini Flash-Lite source review was run through the guarded agent router; it confirmed the same distinction. No credentials, customer datasets, `.env`, or network provider were included in that review.
- Verification: `python -m pytest -q` in `services/research` — **85 passed** before fix; tests explicitly assert source endpoints are 503 and ABR postcode search raises `not implemented`, so this suite does not verify live prospecting. Fixed `_run_pipeline` so a failed discovery step ends as `failed` and skips dependent stages instead of falsely becoming `completed`; added regression test. Same command after fix — **86 passed**.
- Impact: a direct/internal pipeline invocation could previously create a false-success empty search record if discovery failed. The public UI/route guards prevented normal users reaching this path; no live fake records were written. Prevention: the new regression covers overall status and proves downstream steps remain pending.
- No `.env`, credentials, Google Sheet, VPS process, or production deployment changed. Current source: `codex/genlead-shipfix` at `ad372e734167b4b06b639fe1aba492c942239d43`; source/docs/tests are uncommitted in the isolated worktree. Existing `.playwright-cli/` preserved.
- Next: wire only owner-approved data sources (registered ABR access plus an approved public-web search/contact source), finish compliant multi-page crawling and evidence-backed contact extraction, implement durable candidate persistence/dedup/review, test with real provider responses and the dashboard, then seek deployment approval. Until then, prospect search remains not ship-ready.

## 2026-09-25 — public web-scraping implementation and release boundary

- Implemented the actual public ABR search path (no GUID), candidate Sheet persistence/dedupe, run-result restoration, honest source/evaluation states, and dashboard results. Added a Companies-drawer workflow to provide or reuse a company website and crawl robots-allowed public pages for explicitly published contacts/channels. New URLs are marked operator-supplied/unverified, do not overwrite a different saved domain, and are not crawled unless their Sheet save succeeds. Contact response/UI distinguish saved records from pending writes. Failed live Companies/Rejected tab reads now stop the discovery run for review before any candidate writes.
- Live read-only QA: ABR postcode `4000`, industry `Mining` returned 87 active name matches across bounded terms; this is not exhaustive or an industry classification and contains no websites/people. Crawled AE Group's public site (4 permitted same-origin pages): 0 explicit name+role people, 12 generic contact channels. No Sheet or production write was made.
- Automated web search remains unresolved: DuckDuckGo presented a human-verification CAPTCHA in headless automation; it was not solved/bypassed, and its `/html`/`/lite` paths are robots-disallowed. OpenStreetMap's public Overpass guidance says commercial apps should use self-hosted/paid instances; no shared public endpoint was used for production prospecting. The current dashboard therefore uses ABR names plus operator-assisted website crawl; it does not auto-resolve company sites or guarantee contacts.
- Tests: research service `python -m pytest -q` — **107 passed**; `npm run build --workspace=apps/web` — passed; `git diff --check` — passed (line-ending warnings only). No authenticated browser pass was possible with the live SMTP `535` blocker.
- No `.env`, credentials, live Sheets, VPS, production services, commit, push, or deployment was changed. Changes are uncommitted in `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, base HEAD `ad372e734167b4b06b639fe1aba492c942239d43`. Existing `.playwright-cli/` preserved.
- Next: choose/authorize a compliant runtime for automated public-web company-site discovery, resolve SMTP, then perform live signed-in browser and Sheet write QA before any release. Current state is real, useful partial prospect research—not end-to-end ship readiness.

## 2026-09-25 — one-email, one-trusted-browser auth hardening

- Truth labels: **SOURCE-ONLY** for the new auth/device binding; **112 BACKEND TESTS PASS**; **4 TRUSTED-DEVICE TESTS PASS**; **NOT DEPLOYED**; **FIRST DEVICE NOT YET ENROLLED**.
- User requirement: production allowlist must contain only MSV's email, no public account creation, and only the first browser that completes OTP may remain trusted. Other browsers must not replace/revoke it. Normal usage should not keep expiring the user out.
- Implemented in the isolated worktree: an HttpOnly `pi_device` token is randomly generated only after successful email OTP; the on-disk record stores the email and token SHA-256 hash (never the raw token), created exclusively with owner-only file permissions. Login and password-reset requests from other browsers are rejected before sending OTP; race-time enrollment is first-writer-wins; corrupt records fail closed. Logout clears only the session cookie, not the trusted-browser cookie. Session/JWT expiry is 365 days and `/api/auth/me` renews both cookies on app load, visibility return, and a 12-hour timer.
- Boundaries: both Next middleware and FastAPI check the signed `deviceId`, HttpOnly browser cookie, email, and current shared record. Defaults resolve to `apps/web/data/trusted-device.json` in both runtimes; both services must run as the same restricted VPS user. Custom `AUTH_TRUSTED_DEVICE_STORE_PATH` must be the identical absolute path in both processes. The path is gitignored.
- Recovery: lost/cleared browser cookies require an administrator to verify MSV's identity, remove the exact trust record, then have MSV complete OTP on his intended device. This intentionally does not let an unknown browser replace the enrolled device. The lock is to a browser installation/cookie, not hardware attestation; stolen/copied cookies are outside that guarantee.
- Migration note: deploying this version invalidates existing legacy session JWTs that have no `deviceId`; one successful OTP sign-in is required to enroll. Therefore the first post-deploy OTP must be completed on MSV's intended device. No trust record/device has been enrolled yet.
- Verification: Node trusted-device tests cover first claim, second-browser denial/non-overwrite, malformed-record fail-closed, and email/cookie/JWT/store binding: **4 passed**. Research backend suite: **112 passed**. `npm run build --workspace=apps/web`: **passed**. After build completion, `npm run typecheck --workspace=apps/web`: **passed**. `git diff --check` reports only the repository's LF/CRLF notices.
- Independent review: attempted the guarded Claude Sonnet QA lane; launcher returned an organization-policy error (“Claude subscription access disabled”). No Claude review result is claimed. No Gemini review was run.
- Web-source check: Mojeek's terms prohibit automated access to its search service without API authorization and expressly prohibit scraping. The Queensland QBCC contractor register is published under CC BY 4.0 and is a usable official source for licensed construction businesses only; it has no website/decision-maker contact fields and is not a general-sector substitute. No provider/API registration, payment, or scrape was performed. Sources: `https://www.mojeek.com/about/terms.html` and `https://www.data.qld.gov.au/dataset/qbcc-licensed-contractors-register/resource/25608781-b28c-44f8-8545-0ab18d84082f`.
- No `.env`, credential, SMTP setting, production VPS process, Google Sheet, cookie, or inbox content was read or changed. The current browser tool exposed only an isolated `about:blank`, not the authenticated in-app tab; authenticated production UI QA is therefore still unverified.
- Current repo: `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`; branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`; all prior prospecting changes and this auth change remain uncommitted; pre-existing `.playwright-cli/` remains preserved.
- Next: finish sequential typecheck/build checks; confirm which physical browser MSV will use first; ensure OTP delivery works; integrate the current uncommitted branch safely with current `origin/main`; at deployment set `AUTH_ALLOWED_EMAILS` to exactly `Mathysuthen@yahoo.com.au`, deploy only GenLead, and have MSV perform the first successful OTP on his own device. Do not describe all prospecting as ship-ready: automatic official-site discovery remains unresolved.

## 2026-09-25 — global search/map final QA

- Superseding SMTP status: user says OTP arrives for him; prior 535 is historical, not a current blocker. No reset/OTP/send test performed.
- Read-only production browser QA confirms `/search` is still the old QLD/postcode UI, says research is not connected, and has a disabled search button. `/locations` is Queensland-only and says “Map view unavailable”; console shows the Cesium chunk syntax error and `ChunkLoadError`.
- Local uncommitted source implements global free-text search, OSM candidate discovery with limited category/tag filtering, optional QLD ABR, Sheet candidate persistence/dedup flow, and a global Leaflet map. User-facing source warning now describes search as best-effort and non-exhaustive.
- Final local checks: backend **123 passed**; frontend typecheck **passed**; optimized frontend production build **passed** with search and locations routes.
- Realistic bounded business/industry queries to both configured public Overpass endpoints timed out/returned HTTP 504 from the production VPS. Only a small diagnostic query succeeded; that does not establish useful business-search coverage. No production search was submitted, no Sheets were written, no `.env` or credentials touched, and no deployment/restart/commit/push occurred.
- State: repo `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, HEAD `ad372e734167b4b06b639fe1aba492c942239d43`, dirty with prior user changes; `origin/main` two commits ahead. Preserve `.playwright-cli/`. No Graphify project graph; used scoped repository search.
- Release gate remains open: source reliability and broad/accurate company/contact coverage are not established; map/global changes are undeployed; trusted-browser auth enrollment is not confirmed for MSV's own browser. Do not claim production is ready. Secure a reliable permitted source, run provider-backed candidate and Sheet-persistence QA, confirm MSV's target browser, then deploy scoped GenLead changes and test live.

## 2026-09-25 — Overture global prospect search implementation

- Truth labels: **SOURCE IMPLEMENTED**; **128 BACKEND TESTS PASS**; **FRONTEND TYPECHECK AND PRODUCTION BUILD PASS**; **NOT YET DEPLOYED**; **LIVE SHEET WRITE NOT RUN**.
- Added a user-triggered Overture Places adapter: fetches the latest monthly release from the Overture STAC catalog, resolves free-text locations, issues a 5 km bounded DuckDB/Parquet Places query with a curated sector taxonomy filter, limits results to 100 candidates and Sheets writes to 30 new companies, serializes queries, and caches release/results. The research route uses this globally and still adds ABR name matches for QLD postcodes.
- Candidate GERS/provider ID, source release, confidence, operating-status value, full source records and source-level license fields when supplied, Overture attribution guide, map coordinates/address, listed website/phone/email, and unverified-category flags are persisted. New `business_phone` and `business_email` columns append to Companies; the schema migration is append-only. Website crawling still uses the existing robots/SSRF-hardened crawler and only listed or explicitly supplied URLs. Dedupe checks canonical and rejected rows; a regression test covers a rejected provider ID when its display name changes.
- Replaced Cesium with the Leaflet global map already implemented in the worktree and updated Search copy, saved-company contact display, dashboard tour and assistant guide. User chose to defer trusted-device enrollment/lock for this deployment; all auth/device-lock files and session changes are intentionally excluded from the release commit. No `.env`/credentials are opened or changed and no OTP was sent.
- Verification on this worktree: `python -m pytest -q` — **128 passed**; `npm run typecheck --workspace=apps/web` — **passed**; `npm run build --workspace=apps/web` — **passed**, including `/search`, `/locations`, `/companies` and `/settings`. A bounded independent Antigravity/Gemini Flash-Lite static QA pass returned `SOURCE-ONLY`; its outdated fixture mention of ODbL was corrected after checking Overture's official Places licensing/schema docs. No independent runtime claim is derived from that review.
- Authoritative Overture references checked: [Places Guide](https://docs.overturemaps.org/guides/places/), [Place schema](https://docs.overturemaps.org/schema/reference/places/place/), and [Attribution and Licensing](https://docs.overturemaps.org/attribution/). The guide identifies Overture Places as real-world place candidates with source-dependent licenses and says the Places data does not contain OSM data.
- Preflight: worktree is `C:\Users\Asus\Documents\Codex\2026-09-23\for-x20\work\genlead-shipfix`, branch `codex/genlead-shipfix`, base `ad372e734167b4b06b639fe1aba492c942239d43`; `origin/main` (`db25261`) has the same tree. Production checkout is `/opt/frictiongenlead/app` at `72c7093851123ac263ecc7a1490c9c1aed0436e0`; only `frictiongenlead-api` and `frictiongenlead-web` are in that user's PM2 list. Preserved untracked VPS files: `apps/web/.env.production.bak.smtp-swap-20260923-141617`, `ecosystem.config.js`, and `services/research/venv/`. Other VPS apps untouched.
- Next: commit/push only the search/map/Sheet changes (not auth files), merge the focused PR after checks, deploy only Friction GenLead, run a real read-only Overture query from the deployed VPS, smoke-check the named PM2 services/routes, then use the in-app browser for Search/Locations visual QA. If that passes, the only still-unverified part will be a real authenticated search writing candidates to the live Sheet; do not invent company rows for testing.

## 2026-09-25 — VPS smoke-test correction

- Truth: PR #3 is merged as `4a20533` and its code has been pulled to `/opt/frictiongenlead/app`; Python dependencies were installed and the Next production build passed. The two PM2 processes have **not** been restarted.
- The first read-only live Overture query, run before any process restart or Sheet operation, failed immediately because DuckDB rejects `enable_progress_bar` as a connection-level global option. No result query completed and no Sheet rows were read or written by this smoke test.
- Removed the unsupported DuckDB option and added a regression test that captures the connection configuration. Full research backend suite: **129 passed**. This correction is local and not yet merged/deployed.
- Release follow-up: merge this narrow runtime fix, fast-forward the VPS checkout, rerun the same bounded read-only live query, then restart only `frictiongenlead-api` and `frictiongenlead-web` if the query succeeds. Preserve the VPS's existing untracked ecosystem config, Python venv, and SMTP backup; do not touch other PM2 apps.
- Device-lock/session changes remain intentionally deferred per Jeevan. No `.env`, credentials, OTP, or authenticated candidate-to-Sheets production write was used. Full live data-persistence workflow still requires an authenticated user-selected search and is not claimed as verified.
- After PR #4 was merged and pulled, the repeat read-only query passed DuckDB startup and reached the live release but exposed a second schema mismatch: the adapter referenced `taxonomy.basic_category`, while Overture v2 stores `basic_category` at the Place record's top level. No candidate rows were returned and no Sheet operation or PM2 restart occurred. A local code/test correction now uses the top-level field; backend suite remains **129 passed**. This second correction needs its own merge and live rerun.
